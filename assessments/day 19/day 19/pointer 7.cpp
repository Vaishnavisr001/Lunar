//writre program to sort array in descending order using pointer
#include <iostream>
using namespace std;


int* allocMen(int* p, int nEle) {
	p =(int *) malloc(nEle * sizeof(int));
	return p;

}
int getvalues(int* p, int n) {
	for (int i = 0;i < n;i++,p++) {
		cin >> *p;
		return 0;

	}
	int sortAsc(int* p, int n); {
		for (int i = 0;i < n;i++, p++) {

	}
}