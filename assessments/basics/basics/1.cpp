#include<iostream>
using namespace std;
int main()
{
	char str[100];
	int n;
	cin >> n;

	for (int i = 0;str[n]!='\0';i++) {
		if (str[i] >= 'A' && str[i] <= 'Z') {
			str[i]=str[i
		}
		else if(str)

	}
}