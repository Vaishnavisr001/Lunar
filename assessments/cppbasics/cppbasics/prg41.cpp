#include<iostream>
#include<cstring>
using namespace std;
int print(int n, int i = 1) {
	if (i <= n && cout << i && print(n, i + 1)) {}
		return 0;
	}
	int main()
	{
		int n;
		if (cin >> n) {}
		if (print(n)) {}
	}
